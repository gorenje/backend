version: '2'
services:
  kafidx:
    build: ../src/kafidx
    ports:
      - "$KAFIDX_PORT:$KAFIDX_PORT"
    environment:
      - PORT=$KAFIDX_PORT
      - WEB_CONCURRENCY=3
      - ZOOKEEPER_HOST=zookeeper:2181

networks:
  default:
    external:
      name: thenetwork
